---
# Collection requirements for sdd-ansible
# Pin versions per BEST-PRACTICES-SPEC REQ-UNI-61
# Note: ansible.builtin is bundled with ansible-core and not installable via Galaxy

collections:
  - name: ansible.posix
    version: "2.1.0"

  - name: amazon.aws
    version: "9.5.0"
